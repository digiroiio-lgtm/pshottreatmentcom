/** Shared site-wide configuration used across SEO schemas and metadata. */
export const SITE = {
  name: "P-Shot Treatment Turkey",
  url: "https://pshottreatment.com",
  phone: "+905353998999",
  /**
   * Total verified review count — update this whenever the review volume
   * crosses a significant milestone (e.g. 600, 750, 1000).
   */
  reviewCount: 500,
  ratingValue: "5.0",
} as const;
